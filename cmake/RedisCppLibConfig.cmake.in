@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(hiredis REQUIRED)
if (@REDIS_ENABLE_SSL@)
    find_dependency(hiredis_ssl REQUIRED)
endif ()

include("${CMAKE_CURRENT_LIST_DIR}/RedisCppLibTargets.cmake")

check_required_components(RedisCppDriver)
