services:
  redis:
    image: redis:latest
    container_name: redis-cpp-lib-test
    ports:
      - "6379:6379"
    volumes:
      - ./data:/data
    command: redis-server --appendonly yes --requirepass redis-auth-password
    restart: unless-stopped
